# 병커시유 Ocean Lab 웹 소스

최신 동기화: 배포 12판, 소스 커밋 146451f9e23df15751b3099981a803ddf8a3ff82의 추적 파일 126개가 GitHub 소스와 바이트 일치한다. 현재 해시는 [SOURCE_IMPORT.json](ocean-lab/SOURCE_IMPORT.json), 배포 10판 이력은 [보존 manifest](source_import_v10.json)에 있다. 구매/운항 해석과 울산 원자료 검토도 반영됐다. 아래 10판 설명은 당시 수입 이력이다. 이번 작업은 문서·해시 기록 갱신이며 새 웹 배포나 모델 실행이 아니다.

시연: https://bunkering-ocean-lab.hc24734503.chatgpt.site/

## 포함 범위

Sites 배포 10판의 소스 커밋 `ab23b5893d3f409b7237677d0ba1db8fef3bb255`에서 추적된 125개 파일을 `ocean-lab/`에 바이트 그대로 보존했습니다. 원본 소스의 작성 이력은 Sites 저장소에 있으며, GitHub에서는 이번 수입 시점부터 변경을 추적합니다. 원본 전체 Git 이력을 옮겼다는 뜻은 아닙니다. `SOURCE_IMPORT.json`의 파일별 SHA-256으로 동일성을 확인할 수 있습니다.

- Three.js 선박 모형, 안내형 항로 선택→운항 체험→정책 비교 흐름, 항차 기록 재생 및 정책 비교
- 호르무즈·수에즈·희망봉·바브엘만데브·대만해협·도버·울산의 지형 표시 및 지구본
- 원본 1,600 에피소드의 규칙 정책 반복을 제거한 700개 정책/체크포인트/사례 조합, 19,030개 전이. 공유 시장 사례는 100개이며 독립 사례 700개가 아닙니다.
- 지형 및 선박 모형은 설명용입니다. 실제 항차 자료·항만 공급제약·선박 물리모델을 추가한 것이 아닙니다.

## 로컬 실행

Node.js 22.13 이상 및 package.json에 지정된 pnpm 11.25.0을 사용합니다. Windows에서는 Anaconda/Python 환경과 별개인 Node 프로젝트이며, 쉘 스크립트가 필요한 보조 명령은 WSL 또는 Git Bash를 사용합니다.

```sh
cd web/ocean-lab
pnpm install --frozen-lockfile
pnpm dev
```

실행 시 출력된 로컬 주소를 엽니다. 일반 로컬 실행 경로의 기본 개발 포트는 5173입니다. 빌드는 `pnpm build`입니다. `.sites-runtime`은 환경별 임시 실행 설정으로 포함하지 않습니다.

## 배포와 검증 경계

GitHub에 소스를 반영하는 작업과 Sites 배포는 별개입니다. 현재 GitHub push로 웹이 자동 배포되도록 연결하지 않았습니다. 기존 Sites 주소와 접근 권한은 유지합니다. `.openai/hosting.json`은 기존 프로젝트 식별 정보이며 인증 토큰이 아닙니다. `.env`, node_modules, 빌드 결과 및 인증 정보는 수입 대상에 없습니다.

`public/data/integration-review.json`은 이전 웹 데이터 대조 기록입니다. 이번 소스 수입을 새 모델 평가나 새 브라우저·모바일/GPU 검증으로 해석하지 않습니다. 원본 웹 README의 과거 검증 설명도 해당 시점의 이력입니다. 진단 기준 문서는 연구 커밋 `bc3293f06e4118e5d245a716b2402fe7080905b8`을 참조합니다.

## 기여 기록

- 요구사항·수입 승인: 최희찬
- 기존 웹 구현: Sites 원본 Git 이력 기준 Codex 및 해당 커밋의 기여자
- 소스 대조·통합 문서: Codex 지원
- GitHub 통합: 최희찬의 요청으로 연결 계정을 통해 수행. 통합 커밋 계정을 원본 125개 파일의 작성자로 간주하지 않습니다.

## 지역 지형 운항 체험

지역 지도에서 설명용 선박 3척이 통과하며, 선택한 지역을 운항 체험 화면의 배경으로 이어갑니다. 체험 선박 한 척은 기존 30단계 기록에 맞춰 이동합니다. 이동만 보간하고 연료·급유·SCI는 완료된 단계의 원본 값을 사용합니다. 선박 근접·탱크 보기는 유지합니다. 통과선·선박 수·크기는 실제 항적이나 교통량이 아니며 지역 선택은 실험 조건을 바꾸지 않습니다.

이번 변경에서 빌드 및 7개 경로의 도착/조기 종료 위치 계산을 확인했습니다. 브라우저의 SVG 호환 화면에서 호르무즈/수에즈 지역 선택 연계, 2단계 연료 90.0 동기화, 30단계 도착 유지, Fixed Fueling의 20/30 위치·연료 0 종료를 확인했습니다. 실제 모바일 GPU의 WebGL 지형 표현은 미확인입니다.

## 배포 10판 소스 동기화 (2026-09-17)

한국어/영어 전환, 7개 지역의 출발·도착 표식, 도착 시 깃발 색 전환을 배포본과 동일하게 반영했습니다. `Geography.tsx`의 `datasestate` 오타를 `dataset`으로 고친 배포 10판도 포함합니다. 이 오타는 지도 렌더링 루프에서 예외를 발생시켰습니다.

이번 작업은 이미 배포된 소스의 GitHub 동기화입니다. 새 배포나 새 모델 실험을 수행한 것이 아닙니다. 원본 Sites README는 시점별 이력으로 그대로 보존하며, 첫 문장의 Private 표기는 초기 이력입니다. 현재 사이트 접근 설정은 public입니다. 10판 배포 후 사용자가 휴대폰 접속 복구를 확인했으나 모든 모바일/GPU 조합의 검증을 뜻하지 않습니다.

`SOURCE_IMPORT.json`은 README를 포함한 전체 추적 소스의 SHA-256을 기록합니다. `public/data/`와 Python 연구 코드·결과는 이번 동기화에서 변경하지 않았습니다. 북극항로는 후속 연구 후보 설명만 있으며 별도의 체험 화면이나 검증된 실험으로 구현되지 않았습니다.

동기화 검증: 전체 125개 소스 파일의 해시·실행 권한 일치, 번역 310개/수치 자리표시자/7개 깃발 도착·조기 종료·초기화 검사 통과, 프로덕션 빌드 성공. 기존 GitHub 수입에서 누락된 셸 스크립트 4개의 실행 권한도 원본대로 복원했습니다. 이번에는 브라우저·실제 모바일 검사를 새로 수행하지 않았습니다. Python 소스와 연구 산출물은 변경하지 않아 Python 테스트를 재실행하지 않았습니다. 사용자 PC의 기존 main에서는 225개 통과 로그를 확인했습니다.
