# Auto detect text files and perform LF normalization
* text=auto

# Preserve the public source copy byte-for-byte so its documented SHA-256
# remains valid after checkout or git add --renormalize.
data/public/*.csv -text
